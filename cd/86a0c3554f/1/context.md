# Session Context

## User Prompts

### Prompt 1

Implement the following plan:

# Consolidate Semantic Releases Views

## Context
There are two places showing a project's semantic releases: a "Semantic Releases" tab inside the project detail page (`project-detail.tsx`) and a standalone page at `/projects/[id]/semantic-releases`. They show the same data with different styling. The standalone page uses generic shadcn components (Card, Badge, Button) which is inconsistent with the flat design system used everywhere else. We need to remove the tab...

### Prompt 2

Commit change

