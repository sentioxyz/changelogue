I want to create a stealth mode which is designed to integrate with agent harness like claude code. there's no ui, and it starts a local running process with local storage like sqllite, the agent talks to it through the cli

---

Base directory for this skill: /Users/pc/.claude/plugins/cache/superpowers-marketplace/superpowers/5.0.7/skills/brainstorming

# Brainstorming Ideas Into Designs

Help turn ideas into fully formed designs and specs through natural collaborative dialogue.

Start by understanding the current project context, then ask questions one at a time to refine the idea. Once you understand what you're building, present the design and get user approval.

<HARD-GATE>
Do NOT invoke any implementation skill, write any code, scaffold any project, or take any implementation action until you have presented a design and the user has approved it. This applies to EVERY project regardless of perceived simplicity.
</HARD-GATE>

## Anti-Pattern: "This Is Too Simple To Need A Design"

Every project goes through this process. A todo list, a single-function utility, a config change — all of them. "Simple" projects are where unexamined assumptions cause the most wasted work. The design can be short (a few sentences for truly simple projects), but you MUST present it and get approval.

## Checklist

You MUST create a task for each of these items and complete them in order:

1. **Explore project context** — check files, docs, recent commits
2. **Offer visual companion** (if topic will involve visual questions) — this is its own message, not combined with a clarifying question. See the Visual Companion section below.
3. **Ask clarifying questions** — one at a time, understand purpose/constraints/success criteria
4. **Propose 2-3 approaches** — with trade-offs and your recommendation
5. **Present design** — in sections scaled to their complexity, get user approval after each section
6. **Write design doc** — save to `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md` and commit
7. **Spec self-review** — quick inline check for placeholders, contradictions, ambiguity, scope (see below)
8. **User reviews written spec** — ask user to review the spec file before proceeding
9. **Transition to implementation** — invoke writing-plans skill to create implementation plan

## Process Flow

```dot
digraph brainstorming {
    "Explore project context" [shape=box];
    "Visual questions ahead?" [shape=diamond];
    "Offer Visual Companion\n(own message, no other content)" [shape=box];
    "Ask clarifying questions" [shape=box];
    "Propose 2-3 approaches" [shape=box];
    "Present design sections" [shape=box];
    "User approves design?" [shape=diamond];
    "Write design doc" [shape=box];
    "Spec self-review\n(fix inline)" [shape=box];
    "User reviews spec?" [shape=diamond];
    "Invoke writing-plans skill" [shape=doublecircle];

    "Explore project context" -> "Visual questions ahead?";
    "Visual questions ahead?" -> "Offer Visual Companion\n(own message, no other content)" [label="yes"];
    "Visual questions ahead?" -> "Ask clarifying questions" [label="no"];
    "Offer Visual Companion\n(own message, no other content)" -> "Ask clarifying questions";
    "Ask clarifying questions" -> "Propose 2-3 approaches";
    "Propose 2-3 approaches" -> "Present design sections";
    "Present design sections" -> "User approves design?";
    "User approves design?" -> "Present design sections" [label="no, revise"];
    "User approves design?" -> "Write design doc" [label="yes"];
    "Write design doc" -> "Spec self-review\n(fix inline)";
    "Spec self-review\n(fix inline)" -> "User reviews spec?";
    "User reviews spec?" -> "Write design doc" [label="changes requested"];
    "User reviews spec?" -> "Invoke writing-plans skill" [label="approved"];
}
```

**The terminal state is invoking writing-plans.** Do NOT invoke frontend-design, mcp-builder, or any other implementation skill. The ONLY skill you invoke after brainstorming is writing-plans.

## The Process

**Understanding the idea:**

- Check out the current project state first (files, docs, recent commits)
- Before asking detailed questions, assess scope: if the request describes multiple independent subsystems (e.g., "build a platform with chat, file storage, billing, and analytics"), flag this immediately. Don't spend questions refining details of a project that needs to be decomposed first.
- If the project is too large for a single spec, help the user decompose into sub-projects: what are the independent pieces, how do they relate, what order should they be built? Then brainstorm the first sub-project through the normal design flow. Each sub-project gets its own spec → plan → implementation cycle.
- For appropriately-scoped projects, ask questions one at a time to refine the idea
- Prefer multiple choice questions when possible, but open-ended is fine too
- Only one question per message - if a topic needs more exploration, break it into multiple questions
- Focus on understanding: purpose, constraints, success criteria

**Exploring approaches:**

- Propose 2-3 different approaches with trade-offs
- Present options conversationally with your recommendation and reasoning
- Lead with your recommended option and explain why

**Presenting the design:**

- Once you believe you understand what you're building, present the design
- Scale each section to its complexity: a few sentences if straightforward, up to 200-300 words if nuanced
- Ask after each section whether it looks right so far
- Cover: architecture, components, data flow, error handling, testing
- Be ready to go back and clarify if something doesn't make sense

**Design for isolation and clarity:**

- Break the system into smaller units that each have one clear purpose, communicate through well-defined interfaces, and can be understood and tested independently
- For each unit, you should be able to answer: what does it do, how do you use it, and what does it depend on?
- Can someone understand what a unit does without reading its internals? Can you change the internals without breaking consumers? If not, the boundaries need work.
- Smaller, well-bounded units are also easier for you to work with - you reason better about code you can hold in context at once, and your edits are more reliable when files are focused. When a file grows large, that's often a signal that it's doing too much.

**Working in existing codebases:**

- Explore the current structure before proposing changes. Follow existing patterns.
- Where existing code has problems that affect the work (e.g., a file that's grown too large, unclear boundaries, tangled responsibilities), include targeted improvements as part of the design - the way a good developer improves code they're working in.
- Don't propose unrelated refactoring. Stay focused on what serves the current goal.

## After the Design

**Documentation:**

- Write the validated design (spec) to `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`
  - (User preferences for spec location override this default)
- Use elements-of-style:writing-clearly-and-concisely skill if available
- Commit the design document to git

**Spec Self-Review:**
After writing the spec document, look at it with fresh eyes:

1. **Placeholder scan:** Any "TBD", "TODO", incomplete sections, or vague requirements? Fix them.
2. **Internal consistency:** Do any sections contradict each other? Does the architecture match the feature descriptions?
3. **Scope check:** Is this focused enough for a single implementation plan, or does it need decomposition?
4. **Ambiguity check:** Could any requirement be interpreted two different ways? If so, pick one and make it explicit.

Fix any issues inline. No need to re-review — just fix and move on.

**User Review Gate:**
After the spec review loop passes, ask the user to review the written spec before proceeding:

> "Spec written and committed to `<path>`. Please review it and let me know if you want to make any changes before we start writing out the implementation plan."

Wait for the user's response. If they request changes, make them and re-run the spec review loop. Only proceed once the user approves.

**Implementation:**

- Invoke the writing-plans skill to create a detailed implementation plan
- Do NOT invoke any other skill. writing-plans is the next step.

## Key Principles

- **One question at a time** - Don't overwhelm with multiple questions
- **Multiple choice preferred** - Easier to answer than open-ended when possible
- **YAGNI ruthlessly** - Remove unnecessary features from all designs
- **Explore alternatives** - Always propose 2-3 approaches before settling
- **Incremental validation** - Present design, get approval before moving on
- **Be flexible** - Go back and clarify when something doesn't make sense

## Visual Companion

A browser-based companion for showing mockups, diagrams, and visual options during brainstorming. Available as a tool — not a mode. Accepting the companion means it's available for questions that benefit from visual treatment; it does NOT mean every question goes through the browser.

**Offering the companion:** When you anticipate that upcoming questions will involve visual content (mockups, layouts, diagrams), offer it once for consent:
> "Some of what we're working on might be easier to explain if I can show it to you in a web browser. I can put together mockups, diagrams, comparisons, and other visuals as we go. This feature is still new and can be token-intensive. Want to try it? (Requires opening a local URL)"

**This offer MUST be its own message.** Do not combine it with clarifying questions, context summaries, or any other content. The message should contain ONLY the offer above and nothing else. Wait for the user's response before continuing. If they decline, proceed with text-only brainstorming.

**Per-question decision:** Even after the user accepts, decide FOR EACH QUESTION whether to use the browser or the terminal. The test: **would the user understand this better by seeing it than reading it?**

- **Use the browser** for content that IS visual — mockups, wireframes, layout comparisons, architecture diagrams, side-by-side visual designs
- **Use the terminal** for content that is text — requirements questions, conceptual choices, tradeoff lists, A/B/C/D text options, scope decisions

A question about a UI topic is not automatically a visual question. "What does personality mean in this context?" is a conceptual question — use the terminal. "Which wizard layout works better?" is a visual question — use the browser.

If they agree to the companion, read the detailed guide before proceeding:
`skills/brainstorming/visual-companion.md`

---

A, but we should reuse component as we can

---

Yes

---

Yes

---

Yes

---

Yes

---

Yes

---

Yes

---

I think we should make the shell customizable like go to some project, start the claude code to do the update for this depency or analze if there's any breaking change right?

---

Looks good to me

---

Base directory for this skill: /Users/pc/.claude/plugins/cache/superpowers-marketplace/superpowers/5.0.7/skills/writing-plans

# Writing Plans

## Overview

Write comprehensive implementation plans assuming the engineer has zero context for our codebase and questionable taste. Document everything they need to know: which files to touch for each task, code, testing, docs they might need to check, how to test it. Give them the whole plan as bite-sized tasks. DRY. YAGNI. TDD. Frequent commits.

Assume they are a skilled developer, but know almost nothing about our toolset or problem domain. Assume they don't know good test design very well.

**Announce at start:** "I'm using the writing-plans skill to create the implementation plan."

**Context:** This should be run in a dedicated worktree (created by brainstorming skill).

**Save plans to:** `docs/superpowers/plans/YYYY-MM-DD-<feature-name>.md`
- (User preferences for plan location override this default)

## Scope Check

If the spec covers multiple independent subsystems, it should have been broken into sub-project specs during brainstorming. If it wasn't, suggest breaking this into separate plans — one per subsystem. Each plan should produce working, testable software on its own.

## File Structure

Before defining tasks, map out which files will be created or modified and what each one is responsible for. This is where decomposition decisions get locked in.

- Design units with clear boundaries and well-defined interfaces. Each file should have one clear responsibility.
- You reason best about code you can hold in context at once, and your edits are more reliable when files are focused. Prefer smaller, focused files over large ones that do too much.
- Files that change together should live together. Split by responsibility, not by technical layer.
- In existing codebases, follow established patterns. If the codebase uses large files, don't unilaterally restructure - but if a file you're modifying has grown unwieldy, including a split in the plan is reasonable.

This structure informs the task decomposition. Each task should produce self-contained changes that make sense independently.

## Bite-Sized Task Granularity

**Each step is one action (2-5 minutes):**
- "Write the failing test" - step
- "Run it to make sure it fails" - step
- "Implement the minimal code to make the test pass" - step
- "Run the tests and make sure they pass" - step
- "Commit" - step

## Plan Document Header

**Every plan MUST start with this header:**

```markdown
# [Feature Name] Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** [One sentence describing what this builds]

**Architecture:** [2-3 sentences about approach]

**Tech Stack:** [Key technologies/libraries]

---
```

## Task Structure

````markdown
### Task N: [Component Name]

**Files:**
- Create: `exact/path/to/file.py`
- Modify: `exact/path/to/existing.py:123-145`
- Test: `tests/exact/path/to/test.py`

- [ ] **Step 1: Write the failing test**

```python
def test_specific_behavior():
    result = function(input)
    assert result == expected
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/path/test.py::test_name -v`
Expected: FAIL with "function not defined"

- [ ] **Step 3: Write minimal implementation**

```python
def function(input):
    return expected
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/path/test.py::test_name -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add tests/path/test.py src/path/file.py
git commit -m "feat: add specific feature"
```
````

## No Placeholders

Every step must contain the actual content an engineer needs. These are **plan failures** — never write them:
- "TBD", "TODO", "implement later", "fill in details"
- "Add appropriate error handling" / "add validation" / "handle edge cases"
- "Write tests for the above" (without actual test code)
- "Similar to Task N" (repeat the code — the engineer may be reading tasks out of order)
- Steps that describe what to do without showing how (code blocks required for code steps)
- References to types, functions, or methods not defined in any task

## Remember
- Exact file paths always
- Complete code in every step — if a step changes code, show the code
- Exact commands with expected output
- DRY, YAGNI, TDD, frequent commits

## Self-Review

After writing the complete plan, look at the spec with fresh eyes and check the plan against it. This is a checklist you run yourself — not a subagent dispatch.

**1. Spec coverage:** Skim each section/requirement in the spec. Can you point to a task that implements it? List any gaps.

**2. Placeholder scan:** Search your plan for red flags — any of the patterns from the "No Placeholders" section above. Fix them.

**3. Type consistency:** Do the types, method signatures, and property names you used in later tasks match what you defined in earlier tasks? A function called `clearLayers()` in Task 3 but `clearFullLayers()` in Task 7 is a bug.

If you find issues, fix them inline. No need to re-review — just fix and move on. If you find a spec requirement with no task, add the task.

## Execution Handoff

After saving the plan, offer execution choice:

**"Plan complete and saved to `docs/superpowers/plans/<filename>.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?"**

**If Subagent-Driven chosen:**
- **REQUIRED SUB-SKILL:** Use superpowers:subagent-driven-development
- Fresh subagent per task + two-stage review

**If Inline Execution chosen:**
- **REQUIRED SUB-SKILL:** Use superpowers:executing-plans
- Batch execution with checkpoints for review

---

1

---

Base directory for this skill: /Users/pc/.claude/plugins/cache/superpowers-marketplace/superpowers/5.0.7/skills/subagent-driven-development

# Subagent-Driven Development

Execute plan by dispatching fresh subagent per task, with two-stage review after each: spec compliance review first, then code quality review.

**Why subagents:** You delegate tasks to specialized agents with isolated context. By precisely crafting their instructions and context, you ensure they stay focused and succeed at their task. They should never inherit your session's context or history — you construct exactly what they need. This also preserves your own context for coordination work.

**Core principle:** Fresh subagent per task + two-stage review (spec then quality) = high quality, fast iteration

## When to Use

```dot
digraph when_to_use {
    "Have implementation plan?" [shape=diamond];
    "Tasks mostly independent?" [shape=diamond];
    "Stay in this session?" [shape=diamond];
    "subagent-driven-development" [shape=box];
    "executing-plans" [shape=box];
    "Manual execution or brainstorm first" [shape=box];

    "Have implementation plan?" -> "Tasks mostly independent?" [label="yes"];
    "Have implementation plan?" -> "Manual execution or brainstorm first" [label="no"];
    "Tasks mostly independent?" -> "Stay in this session?" [label="yes"];
    "Tasks mostly independent?" -> "Manual execution or brainstorm first" [label="no - tightly coupled"];
    "Stay in this session?" -> "subagent-driven-development" [label="yes"];
    "Stay in this session?" -> "executing-plans" [label="no - parallel session"];
}
```

**vs. Executing Plans (parallel session):**
- Same session (no context switch)
- Fresh subagent per task (no context pollution)
- Two-stage review after each task: spec compliance first, then code quality
- Faster iteration (no human-in-loop between tasks)

## The Process

```dot
digraph process {
    rankdir=TB;

    subgraph cluster_per_task {
        label="Per Task";
        "Dispatch implementer subagent (./implementer-prompt.md)" [shape=box];
        "Implementer subagent asks questions?" [shape=diamond];
        "Answer questions, provide context" [shape=box];
        "Implementer subagent implements, tests, commits, self-reviews" [shape=box];
        "Dispatch spec reviewer subagent (./spec-reviewer-prompt.md)" [shape=box];
        "Spec reviewer subagent confirms code matches spec?" [shape=diamond];
        "Implementer subagent fixes spec gaps" [shape=box];
        "Dispatch code quality reviewer subagent (./code-quality-reviewer-prompt.md)" [shape=box];
        "Code quality reviewer subagent approves?" [shape=diamond];
        "Implementer subagent fixes quality issues" [shape=box];
        "Mark task complete in TodoWrite" [shape=box];
    }

    "Read plan, extract all tasks with full text, note context, create TodoWrite" [shape=box];
    "More tasks remain?" [shape=diamond];
    "Dispatch final code reviewer subagent for entire implementation" [shape=box];
    "Use superpowers:finishing-a-development-branch" [shape=box style=filled fillcolor=lightgreen];

    "Read plan, extract all tasks with full text, note context, create TodoWrite" -> "Dispatch implementer subagent (./implementer-prompt.md)";
    "Dispatch implementer subagent (./implementer-prompt.md)" -> "Implementer subagent asks questions?";
    "Implementer subagent asks questions?" -> "Answer questions, provide context" [label="yes"];
    "Answer questions, provide context" -> "Dispatch implementer subagent (./implementer-prompt.md)";
    "Implementer subagent asks questions?" -> "Implementer subagent implements, tests, commits, self-reviews" [label="no"];
    "Implementer subagent implements, tests, commits, self-reviews" -> "Dispatch spec reviewer subagent (./spec-reviewer-prompt.md)";
    "Dispatch spec reviewer subagent (./spec-reviewer-prompt.md)" -> "Spec reviewer subagent confirms code matches spec?";
    "Spec reviewer subagent confirms code matches spec?" -> "Implementer subagent fixes spec gaps" [label="no"];
    "Implementer subagent fixes spec gaps" -> "Dispatch spec reviewer subagent (./spec-reviewer-prompt.md)" [label="re-review"];
    "Spec reviewer subagent confirms code matches spec?" -> "Dispatch code quality reviewer subagent (./code-quality-reviewer-prompt.md)" [label="yes"];
    "Dispatch code quality reviewer subagent (./code-quality-reviewer-prompt.md)" -> "Code quality reviewer subagent approves?";
    "Code quality reviewer subagent approves?" -> "Implementer subagent fixes quality issues" [label="no"];
    "Implementer subagent fixes quality issues" -> "Dispatch code quality reviewer subagent (./code-quality-reviewer-prompt.md)" [label="re-review"];
    "Code quality reviewer subagent approves?" -> "Mark task complete in TodoWrite" [label="yes"];
    "Mark task complete in TodoWrite" -> "More tasks remain?";
    "More tasks remain?" -> "Dispatch implementer subagent (./implementer-prompt.md)" [label="yes"];
    "More tasks remain?" -> "Dispatch final code reviewer subagent for entire implementation" [label="no"];
    "Dispatch final code reviewer subagent for entire implementation" -> "Use superpowers:finishing-a-development-branch";
}
```

## Model Selection

Use the least powerful model that can handle each role to conserve cost and increase speed.

**Mechanical implementation tasks** (isolated functions, clear specs, 1-2 files): use a fast, cheap model. Most implementation tasks are mechanical when the plan is well-specified.

**Integration and judgment tasks** (multi-file coordination, pattern matching, debugging): use a standard model.

**Architecture, design, and review tasks**: use the most capable available model.

**Task complexity signals:**
- Touches 1-2 files with a complete spec → cheap model
- Touches multiple files with integration concerns → standard model
- Requires design judgment or broad codebase understanding → most capable model

## Handling Implementer Status

Implementer subagents report one of four statuses. Handle each appropriately:

**DONE:** Proceed to spec compliance review.

**DONE_WITH_CONCERNS:** The implementer completed the work but flagged doubts. Read the concerns before proceeding. If the concerns are about correctness or scope, address them before review. If they're observations (e.g., "this file is getting large"), note them and proceed to review.

**NEEDS_CONTEXT:** The implementer needs information that wasn't provided. Provide the missing context and re-dispatch.

**BLOCKED:** The implementer cannot complete the task. Assess the blocker:
1. If it's a context problem, provide more context and re-dispatch with the same model
2. If the task requires more reasoning, re-dispatch with a more capable model
3. If the task is too large, break it into smaller pieces
4. If the plan itself is wrong, escalate to the human

**Never** ignore an escalation or force the same model to retry without changes. If the implementer said it's stuck, something needs to change.

## Prompt Templates

- `./implementer-prompt.md` - Dispatch implementer subagent
- `./spec-reviewer-prompt.md` - Dispatch spec compliance reviewer subagent
- `./code-quality-reviewer-prompt.md` - Dispatch code quality reviewer subagent

## Example Workflow

```
You: I'm using Subagent-Driven Development to execute this plan.

[Read plan file once: docs/superpowers/plans/feature-plan.md]
[Extract all 5 tasks with full text and context]
[Create TodoWrite with all tasks]

Task 1: Hook installation script

[Get Task 1 text and context (already extracted)]
[Dispatch implementation subagent with full task text + context]

Implementer: "Before I begin - should the hook be installed at user or system level?"

You: "User level (~/.config/superpowers/hooks/)"

Implementer: "Got it. Implementing now..."
[Later] Implementer:
  - Implemented install-hook command
  - Added tests, 5/5 passing
  - Self-review: Found I missed --force flag, added it
  - Committed

[Dispatch spec compliance reviewer]
Spec reviewer: ✅ Spec compliant - all requirements met, nothing extra

[Get git SHAs, dispatch code quality reviewer]
Code reviewer: Strengths: Good test coverage, clean. Issues: None. Approved.

[Mark Task 1 complete]

Task 2: Recovery modes

[Get Task 2 text and context (already extracted)]
[Dispatch implementation subagent with full task text + context]

Implementer: [No questions, proceeds]
Implementer:
  - Added verify/repair modes
  - 8/8 tests passing
  - Self-review: All good
  - Committed

[Dispatch spec compliance reviewer]
Spec reviewer: ❌ Issues:
  - Missing: Progress reporting (spec says "report every 100 items")
  - Extra: Added --json flag (not requested)

[Implementer fixes issues]
Implementer: Removed --json flag, added progress reporting

[Spec reviewer reviews again]
Spec reviewer: ✅ Spec compliant now

[Dispatch code quality reviewer]
Code reviewer: Strengths: Solid. Issues (Important): Magic number (100)

[Implementer fixes]
Implementer: Extracted PROGRESS_INTERVAL constant

[Code reviewer reviews again]
Code reviewer: ✅ Approved

[Mark Task 2 complete]

...

[After all tasks]
[Dispatch final code-reviewer]
Final reviewer: All requirements met, ready to merge

Done!
```

## Advantages

**vs. Manual execution:**
- Subagents follow TDD naturally
- Fresh context per task (no confusion)
- Parallel-safe (subagents don't interfere)
- Subagent can ask questions (before AND during work)

**vs. Executing Plans:**
- Same session (no handoff)
- Continuous progress (no waiting)
- Review checkpoints automatic

**Efficiency gains:**
- No file reading overhead (controller provides full text)
- Controller curates exactly what context is needed
- Subagent gets complete information upfront
- Questions surfaced before work begins (not after)

**Quality gates:**
- Self-review catches issues before handoff
- Two-stage review: spec compliance, then code quality
- Review loops ensure fixes actually work
- Spec compliance prevents over/under-building
- Code quality ensures implementation is well-built

**Cost:**
- More subagent invocations (implementer + 2 reviewers per task)
- Controller does more prep work (extracting all tasks upfront)
- Review loops add iterations
- But catches issues early (cheaper than debugging later)

## Red Flags

**Never:**
- Start implementation on main/master branch without explicit user consent
- Skip reviews (spec compliance OR code quality)
- Proceed with unfixed issues
- Dispatch multiple implementation subagents in parallel (conflicts)
- Make subagent read plan file (provide full text instead)
- Skip scene-setting context (subagent needs to understand where task fits)
- Ignore subagent questions (answer before letting them proceed)
- Accept "close enough" on spec compliance (spec reviewer found issues = not done)
- Skip review loops (reviewer found issues = implementer fixes = review again)
- Let implementer self-review replace actual review (both are needed)
- **Start code quality review before spec compliance is ✅** (wrong order)
- Move to next task while either review has open issues

**If subagent asks questions:**
- Answer clearly and completely
- Provide additional context if needed
- Don't rush them into implementation

**If reviewer finds issues:**
- Implementer (same subagent) fixes them
- Reviewer reviews again
- Repeat until approved
- Don't skip the re-review

**If subagent fails task:**
- Dispatch fix subagent with specific instructions
- Don't try to fix manually (context pollution)

## Integration

**Required workflow skills:**
- **superpowers:using-git-worktrees** - REQUIRED: Set up isolated workspace before starting
- **superpowers:writing-plans** - Creates the plan this skill executes
- **superpowers:requesting-code-review** - Code review template for reviewer subagents
- **superpowers:finishing-a-development-branch** - Complete development after all tasks

**Subagents should use:**
- **superpowers:test-driven-development** - Subagents follow TDD for each task

**Alternative workflow:**
- **superpowers:executing-plans** - Use for parallel session instead of same-session execution

---

This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Analysis:
Let me chronologically analyze the conversation:

1. **User's Initial Request**: Create a "stealth mode" for Changelogue that integrates with agent harnesses like Claude Code. No UI, local running process with SQLite, agent talks to it through CLI.

2. **Brainstorming Phase**: Used the brainstorming skill to explore the idea. A subagent explored the entire project architecture and produced a comprehensive summary of the codebase.

3. **Clarifying Questions**:
   - Use case: Event-driven with callback or start new agent instance
   - Lifecycle: System service (always on, agent connects when needed)
   - Callbacks: Shell command callback
   - Data scope: Core subset (sources, releases, polling, notifications)
   - Storage: User home directory (~/.changelogue/stealth.db)
   - API surface: Local HTTP server on localhost:PORT

4. **Three Approaches Proposed**:
   - A: Separate binary with SQLite store (Recommended)
   - B: Single binary with --stealth flag
   - C: Embedded mode via CLI subcommand
   - User chose A, but said "we should reuse component as we can"

5. **Design Sections Presented and Approved**:
   - Section 1: Architecture overview
   - Section 2: SQLite store & schema
   - Section 3: Shell command callbacks
   - Section 4: Synchronous job processing
   - Section 5: Binary, lifecycle & CLI integration
   - Section 6: Ingestion orchestrator reuse

6. **User Feedback on Shell Callbacks**: User wanted the shell to be customizable - "go to some project, start the claude code to do the update for this dependency or analyze if there's any breaking change". This led to per-subscription commands design.

7. **Design Doc Written**: `docs/plans/2026-04-01-stealth-mode-design.md`

8. **Implementation Plan Written**: `docs/plans/2026-04-01-stealth-mode-plan.md` with 13 tasks

9. **Execution**: User chose subagent-driven development (option 1)

10. **Tasks Completed So Far**:
    - Task 1: Add Config field to Subscription model ✅
    - Task 2: Shell Sender ✅
    - Task 3: Refactor Ingestion Orchestrator ✅
    - Task 4: SQLite Store — Schema and Connection ✅
    - Tasks 5+6: Projects + Sources CRUD ✅
    - Task 7: Releases, Channels, Subscriptions, KeyStore, HealthChecker ✅

11. **Tasks Remaining**:
    - Task 8: IngestRelease + NotifyStore + notify.go
    - Task 9: Stub implementations
    - Task 10: Stealth binary main entrypoint
    - Task 11: Integration test
    - Task 12: CLI --config flag
    - Task 13: Final verification

Let me now trace all the specific files and key code changes made.

Key files modified/created:
- `internal/models/subscription.go` - Added Config field
- `internal/db/migrations.go` - Added subscription config migration
- `internal/api/pgstore.go` - Updated all 7 subscription methods for config column
- `internal/routing/shell_sender.go` - New ShellSender implementation
- `internal/routing/shell_sender_test.go` - Tests for ShellSender
- `internal/routing/worker.go` - Added "shell" to NewSenders()
- `internal/ingestion/loader.go` - Extracted SourceLister interface, refactored SourceLoader
- `internal/ingestion/orchestrator.go` - Extracted PollStatusUpdater interface
- `internal/ingestion/pgstore.go` - Added ListEnabledSources, UpdateSourcePollStatus
- `internal/ingestion/service.go` - Updated isUniqueViolation for SQLite
- `cmd/server/main.go` - Updated orchestrator construction
- `internal/stealth/store.go` - New SQLite store (Projects, Sources, Releases, Channels, Subscriptions, KeyStore, HealthChecker)
- `internal/stealth/migrations.go` - SQLite schema
- `internal/stealth/store_test.go` - Tests for all store methods

No errors were encountered during implementation. All tasks completed with passing tests.

The current work was just completing Task 7, and the next task would be Task 8 (IngestRelease + NotifyStore + notify.go).

Summary:
1. Primary Request and Intent:
   The user wants to create a **"stealth mode"** for the Changelogue project — a headless, agent-native operation mode designed to integrate with agent harnesses like Claude Code. Key requirements:
   - No UI — purely CLI-driven
   - Runs as a local system service (always on, agent connects when needed)
   - Uses SQLite for local storage instead of PostgreSQL
   - Shell command callbacks for event-driven notifications (configurable per subscription)
   - Agents can be spawned or resumed when new releases are detected
   - Core feature subset only (sources, releases, polling, notifications — no agent/LLM, gates, todos)
   - Per-subscription command templates so different sources trigger different agent workflows (e.g., "analyze breaking changes" vs "auto-update dependency")
   - Maximize reuse of existing components

2. Key Technical Concepts:
   - **Separate binary architecture**: `cmd/stealth/main.go` builds to `clog-stealth`, reusing existing packages
   - **SQLite via `modernc.org/sqlite`**: Pure Go, no CGo. WAL mode, busy_timeout, foreign_keys pragmas
   - **Interface extraction for database abstraction**: `SourceLister`, `PollStatusUpdater` interfaces allow both PgStore and SQLite store
   - **Synchronous job processing**: Replaces River async queue with in-process notification dispatch
   - **Shell command callbacks**: New `ShellSender` implementing `routing.Sender` interface with `SendWithConfig` for per-subscription commands
   - **Per-subscription config**: `Config json.RawMessage` field added to `models.Subscription` for command templates
   - **Environment variable injection**: `CHANGELOGUE_VERSION`, `CHANGELOGUE_REPOSITORY`, etc. injected into shell commands
   - **Stub pattern**: Unsupported interfaces (SemanticReleases, Agent, Todos, etc.) return 501
   - **Store interface compliance**: SQLite store implements same interfaces as `api.PgStore`
   - **Go module**: `github.com/sentioxyz/changelogue`, Go 1.25

3. Files and Code Sections:

   - **`docs/plans/2026-04-01-stealth-mode-design.md`** — Design spec document covering architecture, SQLite store, shell callbacks, synchronous processing, orchestrator refactor, binary lifecycle. Written and approved by user.

   - **`docs/plans/2026-04-01-stealth-mode-plan.md`** — 13-task implementation plan with TDD steps, exact code, and commit messages.

   - **`internal/models/subscription.go`** (Modified — Task 1)
     - Added `Config json.RawMessage` field for per-subscription shell command configuration
     ```go
     type Subscription struct {
         ID            string          `json:"id"`
         ChannelID     string          `json:"channel_id"`
         Type          string          `json:"type"`
         SourceID      *string         `json:"source_id,omitempty"`
         ProjectID     *string         `json:"project_id,omitempty"`
         VersionFilter string          `json:"version_filter,omitempty"`
         Config        json.RawMessage `json:"config,omitempty"`
         CreatedAt     time.Time       `json:"created_at"`
     }
     ```

   - **`internal/db/migrations.go`** (Modified — Task 1)
     - Added `ALTER TABLE subscriptions ADD COLUMN IF NOT EXISTS config JSONB;` migration

   - **`internal/api/pgstore.go`** (Modified — Task 1)
     - Updated all 7 subscription query methods (ListSubscriptions, CreateSubscription, GetSubscription, UpdateSubscription, CreateSubscriptionBatch, ListSourceSubscriptions, ListProjectSubscriptions) to include `config` column

   - **`internal/routing/shell_sender.go`** (Created — Task 2)
     - `ShellSender` struct implementing `Sender` interface
     - `Send()` — no-op with warning (interface compliance)
     - `SendWithConfig()` — full implementation: parses channel config (defaults) and subscription config (command + overrides), merges timeout/workdir, injects CHANGELOGUE_* env vars, expands ${VAR} in command, runs via `exec.CommandContext` in goroutine
     ```go
     type ShellSender struct{}
     func (s *ShellSender) Send(ctx context.Context, ch *models.NotificationChannel, msg Notification) error
     func (s *ShellSender) SendWithConfig(ctx context.Context, ch *models.NotificationChannel, msg Notification, subConfig json.RawMessage) error
     ```

   - **`internal/routing/shell_sender_test.go`** (Created — Task 2)
     - `TestShellSender_Send` — verifies command runs with env var substitution
     - `TestShellSender_SendMissingCommand` — verifies error on missing command

   - **`internal/routing/worker.go`** (Modified — Task 2)
     - Added `"shell": &ShellSender{}` to `NewSenders()` map

   - **`internal/ingestion/loader.go`** (Modified — Task 3)
     - Extracted `SourceLister` interface and `EnabledSource` struct
     - Refactored `SourceLoader` to accept `SourceLister` instead of `*pgxpool.Pool`
     - Removed `LookupSourceID` (confirmed unused)
     ```go
     type SourceLister interface {
         ListEnabledSources(ctx context.Context) ([]EnabledSource, error)
     }
     type EnabledSource struct {
         ID, Provider, Repository string
         PollIntervalSeconds      int
         LastPolledAt             *time.Time
     }
     ```

   - **`internal/ingestion/orchestrator.go`** (Modified — Task 3)
     - Extracted `PollStatusUpdater` interface
     - Replaced `pool *pgxpool.Pool` with `pollUpdater PollStatusUpdater`
     ```go
     type PollStatusUpdater interface {
         UpdateSourcePollStatus(ctx context.Context, id string, pollErr error) error
     }
     func NewOrchestrator(service *Service, loader *SourceLoader, pollUpdater PollStatusUpdater, interval time.Duration) *Orchestrator
     ```

   - **`internal/ingestion/pgstore.go`** (Modified — Task 3)
     - Added `ListEnabledSources` (implements SourceLister)
     - Added `UpdateSourcePollStatus` (implements PollStatusUpdater)

   - **`internal/ingestion/service.go`** (Modified — Task 3)
     - Updated `isUniqueViolation` to also match SQLite's `"UNIQUE constraint failed"`

   - **`cmd/server/main.go`** (Modified — Task 3)
     - Updated orchestrator construction: `ingestionStore` replaces `pool` in `NewSourceLoader` and `NewOrchestrator`

   - **`internal/stealth/migrations.go`** (Created — Task 4)
     - SQLite schema for: projects, sources, releases, notification_channels, subscriptions, api_keys
     - TEXT for timestamps, INTEGER for booleans, TEXT for JSON

   - **`internal/stealth/store.go`** (Created — Tasks 4-7)
     - `Store` struct wrapping `*sql.DB`
     - `NewStore(dbPath)` — creates directory, opens SQLite, sets pragmas, runs migrations
     - **Projects CRUD**: ListProjects, CreateProject, GetProject, UpdateProject, DeleteProject
     - **Sources CRUD**: ListSourcesByProject, CreateSource, GetSource, UpdateSource, DeleteSource, UpdateSourcePollStatus, ListAllSourceRepos, ListEnabledSources
     - **Releases read**: ListAllReleases, ListReleasesBySource, ListReleasesByProject, GetRelease (with JOIN to sources/projects for enriched fields)
     - **Channels CRUD**: ListChannels, CreateChannel, GetChannel, UpdateChannel, DeleteChannel
     - **Subscriptions CRUD**: ListSubscriptions, CreateSubscription, CreateSubscriptionBatch, GetSubscription, UpdateSubscription, DeleteSubscription, DeleteSubscriptionBatch
     - **KeyStore**: ValidateKey (SHA-256 hash comparison), TouchKeyUsage
     - **HealthChecker**: PingDB, GetStats, GetTrend

   - **`internal/stealth/store_test.go`** (Created — Tasks 4-7)
     - Tests: TestNewStore, TestNewStore_CreatesDirectory, TestProjects_CRUD, TestSources_CRUD, TestReleases_Read, TestChannels_CRUD, TestSubscriptions_CRUD, TestKeyStore, TestHealthChecker

4. Errors and Fixes:
   - No errors were encountered during implementation. All tasks completed successfully with passing tests.
   - All subagent implementations compiled and passed tests on first attempt.

5. Problem Solving:
   - **Database abstraction**: Solved by extracting `SourceLister` and `PollStatusUpdater` interfaces from the ingestion orchestrator, allowing both PostgreSQL and SQLite stores to be used
   - **River queue elimination**: Replaced async transactional outbox with synchronous in-process notification dispatch
   - **Per-subscription customization**: Solved by adding `Config json.RawMessage` to subscriptions and having `ShellSender.SendWithConfig()` accept subscription config separately from channel config
   - **SQLite compatibility**: Handled via `?` placeholders, TEXT timestamps, INTEGER booleans, `UNIQUE constraint failed` error matching

6. All User Messages:
   - "I want to create a stealth mode which is designed to integrate with agent harness like claude code. there's no ui, and it starts a local running process with local storage like sqllite, the agent talks to it through the cli"
   - "event driven and it should be able to use callback or start a new agent instance" (selected Event-driven assistant use case)
   - "System service" (selected system service lifecycle)
   - "Shell command callback" (selected shell command callback for notifications)
   - "Core subset (Recommended)" (selected core feature subset)
   - "User home directory (Recommended)" (selected storage location)
   - "Local HTTP server (Recommended)" (selected API surface)
   - "A, but we should reuse component as we can" (chose separate binary approach with max reuse)
   - "Yes" (approved architecture overview)
   - "Yes" (approved SQLite store & schema section)
   - "Yes" (approved shell command callbacks section)
   - "Yes" (approved synchronous job processing section)
   - "Yes" (approved binary & lifecycle section)
   - "Yes" (approved ingestion orchestrator reuse section)
   - "I think we should make the shell customizable like go to some project, start the claude code to do the update for this depency or analze if there's any breaking change right?" (requested per-subscription command customization)
   - "Per-subscription commands (Recommended)" (selected per-subscription command approach)
   - "Looks good to me" (approved the written spec)
   - "1" (chose subagent-driven development for execution)

7. Pending Tasks (from the 13-task plan):
   - **Task 8**: SQLite Store — IngestRelease + NotifyStore + notify.go (implements ingestion.ReleaseStore, routing.NotifyStore, synchronous notification dispatcher)
   - **Task 9**: Stub implementations for unsupported interfaces (stubs.go)
   - **Task 10**: Stealth binary main entrypoint (cmd/stealth/main.go + Makefile)
   - **Task 11**: Integration test (end-to-end stealth flow)
   - **Task 12**: CLI --config flag for subscriptions
   - **Task 13**: Final verification and cleanup

8. Current Work:
   Task 7 just completed successfully. The subagent-driven development workflow is executing the implementation plan in `docs/plans/2026-04-01-stealth-mode-plan.md`. Seven of thirteen tasks are complete (Tasks 1-7). All completed tasks have been spec-reviewed and passed. The task tracking shows:
   - Tasks 9-11 (plan Tasks 1-3): completed ✅
   - Task 12 (plan Task 4): completed ✅
   - Tasks 13-14 (plan Tasks 5-6): completed ✅
   - Task 15 (plan Task 7): completed ✅
   - Tasks 16-21 (plan Tasks 8-13): pending

   Git commits made so far:
   - `f1205e82` — feat: add config field to subscription model
   - (Task 2 commit) — feat: add ShellSender for shell command callback notifications
   - `d46b7d62` — refactor: extract SourceLister and PollStatusUpdater interfaces
   - `310b3e8a` — feat(stealth): add SQLite store with schema migrations
   - `acbd4e79` — feat(stealth): implement Projects and Sources CRUD on SQLite store
   - (Task 7 commit) — feat(stealth): implement Releases, Channels, Subscriptions, KeyStore, HealthChecker

9. Optional Next Step:
   Continue the subagent-driven development workflow by dispatching **Task 8: SQLite Store — IngestRelease + NotifyStore + notify.go**. This task implements:
   - `IngestRelease()` on the SQLite store (insert release + trigger notify hook)
   - `NotifyStore` methods (ListSourceSubscriptions, GetPreviousRelease, EnqueueAgentRun no-op, CreateReleaseTodo no-op, HasReleaseGate always false)
   - `internal/stealth/notify.go` — synchronous notification dispatcher using `routing.Sender` implementations
   - `NotifyHook` callback pattern on the Store struct
   
   The user chose option "1" (subagent-driven development) and the workflow is actively executing the plan task-by-task with spec and code quality reviews between tasks.

If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/pc/.claude/projects/-Users-pc-web3-ReleaseBeacon/45b32cb4-489b-4cac-803e-86ad19ea5cbd.jsonl
Please continue the conversation from where we left off without asking the user any further questions. Continue with the last task that you were asked to work on.

---

Please update related docs and pipelibnes

---

This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Analysis:
Let me chronologically analyze the conversation:

1. **Session Start**: This is a continuation of a previous conversation. The summary from the prior conversation indicates:
   - Building a "stealth mode" for Changelogue - a headless, agent-native operation mode
   - 13 tasks from an implementation plan, with Tasks 1-7 completed in the prior session
   - The user chose subagent-driven development

2. **Continuation**: The assistant resumed from where it left off, starting with Task 8.

3. **Task 8: IngestRelease + NotifyStore + notify.go**
   - Dispatched a subagent implementer
   - Added `NotifyHook` field to Store struct (public field, not SetNotifyHook method)
   - Implemented `IngestRelease` - generates UUID, marshals metadata into JSON raw_data, inserts release, calls NotifyHook
   - Implemented NotifyStore methods: ListSourceSubscriptions, GetPreviousRelease, EnqueueAgentRun (no-op), CreateReleaseTodo (no-op), HasReleaseGate (always false)
   - Created `internal/stealth/notify.go` - synchronous notification dispatcher
   - Tests: TestIngestRelease, TestNotifyStoreMethods - all passed
   - Status: DONE, committed

4. **Tasks 9 and 12 (parallel)**:
   - Task 9: Created `internal/stealth/stubs.go` with stub implementations for 7 unsupported interfaces
   - Task 12: Added `--config` flag to CLI subscription create command
   - Both compiled successfully

5. **Task 10: Stealth Binary**:
   - Created `cmd/stealth/main.go` - full binary entrypoint
   - Updated Makefile with stealth targets
   - Compiled successfully

6. **Task 11: Integration Test**:
   - Created `internal/stealth/integration_test.go`
   - End-to-end test: API server setup, project/source/channel/subscription creation via HTTP, release ingestion, verification
   - Passed

7. **Task 13: Final Verification**:
   - `go vet ./...` - clean
   - All three binaries build (changelogue, clog, clog-stealth)
   - `go test ./...` - all packages pass
   - All 13 tasks completed

8. **User's second request**: "Please update related docs and pipelines"
   - The assistant dispatched an exploration subagent to find all docs and pipelines
   - Found: README.md, ARCH.md, DESIGN.md, API.md, .github/workflows/ci.yml, deploy.yml, release.yml, .goreleaser.yaml, Dockerfile, docker-compose.yml, Makefile
   - Read the files to understand what needs updating
   - Read README.md, ARCH.md, .goreleaser.yaml in full

9. **Where we left off**: The assistant had just read README.md, ARCH.md, and .goreleaser.yaml and was about to start making the actual updates when the summary was requested.

Key files that need updating:
- `.goreleaser.yaml` - Add clog-stealth build entry (HIGH priority)
- `README.md` - Add stealth mode section, env vars, commands, project structure (HIGH priority)
- `ARCH.md` - Add stealth mode as alternative operation mode, update directory structure (MEDIUM priority)
- `DESIGN.md` - Document stealth mode architecture (MEDIUM priority)
- `API.md` - Note stealth limitations (MEDIUM priority)
- `.github/workflows/ci.yml` - Add stealth build step (MEDIUM priority)

The user's request was explicit: "Please update related docs and pipelines" - this is what was actively being worked on.

Let me trace through all the files modified/created in this session:

**Task 8 files:**
- `internal/stealth/store.go` - Modified: Added NotifyHook field, IngestRelease, ListSourceSubscriptions, GetPreviousRelease, EnqueueAgentRun, CreateReleaseTodo, HasReleaseGate
- `internal/stealth/notify.go` - Created: synchronous notification dispatcher
- `internal/stealth/store_test.go` - Modified: Added TestIngestRelease, TestNotifyStoreMethods

**Task 9 files:**
- `internal/stealth/stubs.go` - Created: 7 stub types (SemanticReleasesStub, AgentStub, TodosStub, OnboardStub, GatesStub, ContextSourcesStub, SessionValidatorStub)

**Task 10 files:**
- `cmd/stealth/main.go` - Created: stealth binary entrypoint
- `Makefile` - Modified: Added stealth and stealth-run targets

**Task 11 files:**
- `internal/stealth/integration_test.go` - Created: end-to-end integration test

**Task 12 files:**
- `internal/cli/subscriptions.go` - Modified: Added --config flag

No errors were encountered during implementation in this session - all subagent tasks completed successfully on first attempt.

Git commits made in this session:
- `9cb21fbf` - feat(stealth): add IngestRelease, NotifyStore methods, and synchronous notify dispatcher
- `9e7d194a` - feat(cli): add --config flag to subscription create command
- `7c9c80c0` - feat(stealth): add stub implementations for unsupported store interfaces
- `2b029802` - feat(stealth): add stealth binary entrypoint and Makefile targets
- `f458687d` - test(stealth): add end-to-end integration test for stealth mode flow

Untracked files noted:
- `.next/`
- `cli`
- `docs/plans/2026-04-01-stealth-mode-design.md`
- `docs/plans/2026-04-01-stealth-mode-plan.md`
- `stealth`

Summary:
1. Primary Request and Intent:
   The user's original request (from prior session) was to create a "stealth mode" for Changelogue — a headless, agent-native operation mode with SQLite storage, no UI, and shell command callbacks for integration with agent harnesses like Claude Code. This was implemented across 13 tasks using subagent-driven development.

   The user's most recent request is: **"Please update related docs and pipelines"** — to update all documentation files (README.md, ARCH.md, DESIGN.md, API.md) and CI/CD pipelines (.goreleaser.yaml, .github/workflows/ci.yml) to reflect the new stealth mode feature.

2. Key Technical Concepts:
   - **Stealth mode**: Headless, agent-native Changelogue mode using SQLite instead of PostgreSQL, synchronous notifications instead of River queue, shell command callbacks
   - **Separate binary architecture**: `cmd/stealth/main.go` builds to `clog-stealth`
   - **SQLite via `modernc.org/sqlite`**: Pure Go, WAL mode, `?` placeholders, TEXT timestamps
   - **Interface extraction**: `SourceLister`, `PollStatusUpdater` for database abstraction
   - **Synchronous job processing**: Replaces River async queue with in-process dispatch
   - **Shell command callbacks**: `ShellSender` with `SendWithConfig` for per-subscription commands
   - **NotifyHook pattern**: Public field on Store struct called after IngestRelease
   - **Stub pattern**: Unsupported interfaces return `errNotImplemented`
   - **Go module**: `github.com/sentioxyz/changelogue`, Go 1.25
   - **Subagent-driven development**: Fresh subagent per task with spec/quality review

3. Files and Code Sections:

   - **`internal/stealth/store.go`** (Modified in Tasks 4-8)
     - Core SQLite store implementing all supported interfaces
     - Added in this session: `NotifyHook` field, `IngestRelease`, `ListSourceSubscriptions`, `GetPreviousRelease`, `EnqueueAgentRun` (no-op), `CreateReleaseTodo` (no-op), `HasReleaseGate` (always false)
     - Key: Store struct now has public `NotifyHook func(ctx context.Context, releaseID, sourceID string)`
     - IngestRelease calls `s.NotifyHook(ctx, id, sourceID)` after successful insert
     ```go
     type Store struct {
         db         *sql.DB
         NotifyHook func(ctx context.Context, releaseID, sourceID string)
     }
     ```

   - **`internal/stealth/notify.go`** (Created in Task 8)
     - Synchronous notification dispatcher reusing `routing.Sender` implementations
     - Handles shell senders specially via `SendWithConfig` with per-subscription config
     ```go
     func (s *Store) NotifyRelease(ctx context.Context, releaseID, sourceID string, senders map[string]routing.Sender) {
         // Gets release, source, checks version filters, iterates subscriptions, dispatches via senders
         // Shell senders get SendWithConfig with sub.Config for per-subscription commands
     }
     ```

   - **`internal/stealth/store_test.go`** (Modified in Task 8)
     - Added `TestIngestRelease` and `TestNotifyStoreMethods`
     - Tests verify IngestRelease, duplicate detection (UNIQUE constraint), ListSourceSubscriptions, GetPreviousRelease, and no-op stubs

   - **`internal/stealth/stubs.go`** (Created in Task 9)
     - 7 stub types: `SemanticReleasesStub`, `AgentStub`, `TodosStub`, `OnboardStub`, `GatesStub`, `ContextSourcesStub`, `SessionValidatorStub`
     - Each returns `errNotImplemented` for all methods
     - Compile-time interface checks: `var _ api.XxxStore = (*XxxStub)(nil)`

   - **`cmd/stealth/main.go`** (Created in Task 10)
     - Full binary entrypoint with subcommands: serve, install, uninstall, status
     - Wires: SQLite store → ingestion service → orchestrator → senders → NotifyHook → API routes
     - Serves on `localhost:9876` by default
     - `store.NotifyHook` wired as a closure calling `store.NotifyRelease`
     - PID file written to `~/.changelogue/stealth.pid`

   - **`Makefile`** (Modified in Task 10)
     - Added `stealth` and `stealth-run` targets
     - Updated `.PHONY` and `clean` target to include `clog-stealth`

   - **`internal/stealth/integration_test.go`** (Created in Task 11)
     - End-to-end test using `httptest.NewServer` with full API route registration
     - Tests: project creation, source creation, channel creation, subscription with config, release ingestion, API verification

   - **`internal/cli/subscriptions.go`** (Modified in Task 12)
     - Added `--config` flag to subscription create command
     - Parses JSON string and includes it as `body["config"]`

   **Files read for docs/pipelines update (not yet modified):**
   - **`.goreleaser.yaml`** — Currently builds `changelogue` and `clog`, needs `clog-stealth` build entry
   - **`README.md`** — Needs stealth mode section, env vars, commands, project structure update
   - **`ARCH.md`** — Needs stealth mode as alternative operation mode, directory structure update

4. Errors and fixes:
   - No errors were encountered during this session. All subagent implementations compiled and passed tests on first attempt.
   - The plan referred to `SetNotifyHook` method but the Task 8 subagent implemented it as a public `NotifyHook` field on the Store struct. The Task 10 subagent correctly used the public field pattern (`store.NotifyHook = func(...)` instead of `store.SetNotifyHook(...)`).

5. Problem Solving:
   - All 13 implementation tasks completed successfully through subagent-driven development
   - Final verification confirmed: all tests pass (`go test ./...`), `go vet` clean, all 3 binaries build
   - 11 git commits made on `main` branch (ahead of origin by 11 commits)
   - Untracked files: `docs/plans/2026-04-01-stealth-mode-design.md`, `docs/plans/2026-04-01-stealth-mode-plan.md` (design docs not committed)
   - Now working on docs/pipelines update per user's latest request

6. All user messages:
   - (From prior session, summarized): Multiple messages brainstorming stealth mode design, approving sections, choosing approaches, and selecting subagent-driven development
   - "Please update related docs and pipelibnes" — The user's most recent request to update documentation and CI/CD pipelines for the stealth mode feature

7. Pending Tasks:
   - Update `.goreleaser.yaml` to add `clog-stealth` build entry (HIGH priority)
   - Update `README.md` with stealth mode section — CLI commands, env vars, build targets, quick start (HIGH priority)
   - Update `ARCH.md` with stealth mode as alternative operation mode, directory structure (MEDIUM priority)
   - Update `DESIGN.md` to document stealth mode architecture (MEDIUM priority)
   - Update `API.md` to note stealth CLI as alternative interface (MEDIUM priority)
   - Update `.github/workflows/ci.yml` to add stealth binary build verification (MEDIUM priority)
   - Commit the design docs (`docs/plans/2026-04-01-stealth-mode-design.md`, `docs/plans/2026-04-01-stealth-mode-plan.md`) which are currently untracked

8. Current Work:
   The user requested: **"Please update related docs and pipelines"**. The assistant dispatched an exploration subagent that identified all files needing updates (README.md, ARCH.md, DESIGN.md, API.md, .goreleaser.yaml, ci.yml, etc.) with a priority-ordered update plan. The assistant then read the three highest-priority files:
   - `.goreleaser.yaml` (65 lines) — needs `clog-stealth` build entry
   - `README.md` (193 lines) — needs stealth mode section, env vars, project structure
   - `ARCH.md` (311 lines) — needs stealth mode as new layer, directory structure

   The actual updates to these files had NOT yet begun when the summary was requested. The assistant was about to start modifying these files.

9. Optional Next Step:
   Continue the docs/pipelines update work by modifying the files identified. The user said: **"Please update related docs and pipelines"**. The high-priority files to update first are:
   1. `.goreleaser.yaml` — Add `clog-stealth` build entry alongside `changelogue` and `clog`
   2. `README.md` — Add stealth mode section with setup instructions, environment variables (`CHANGELOGUE_STEALTH_DB`, `CHANGELOGUE_STEALTH_PORT`, `POLL_INTERVAL`), CLI commands, and update project structure
   3. `ARCH.md` — Add stealth mode as Section 2.x, update architecture diagram, update directory structure
   4. `.github/workflows/ci.yml` — Add `make stealth` build step
   5. Then commit all docs updates

If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/pc/.claude/projects/-Users-pc-web3-ReleaseBeacon/45b32cb4-489b-4cac-803e-86ad19ea5cbd.jsonl
Please continue the conversation from where we left off without asking the user any further questions. Continue with the last task that you were asked to work on.

---

cool, push the changes

---

I noticed that you say it can be embeded to the cicd how?

---

release 0.2.0